CREATE TABLE idea (
                      id serial primary key,
                      time timestamp,
                      abonent text,
                      bo text,
                      name text,
                      status varchar(1),
                      count varchar
)